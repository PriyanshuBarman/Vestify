name: Apply Stock SIP Change Cron

on:
  schedule:
    - cron: "45 19 * * *" # 1:15 AM IST
    - cron: "15 20 * * *" # 1:45 AM IST
  workflow_dispatch: # Allow manual trigger

defaults:
  run:
    working-directory: backend

jobs:
  apply-stock-sip-change-cron:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: "./backend/package-lock.json"

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Apply Stock SIP Change
        id: update-script
        run: npm run apply-stock-sip-changes
        env:
          NODE_ENV: cron
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
